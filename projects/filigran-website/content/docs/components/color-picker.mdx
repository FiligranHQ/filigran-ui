---
title: Color picker
---

## Color picker Example

<ReactLiveDisplay
  codeExample={`() => {
    const [color, setColor] = React.useState('#F9DC7B');
    return (
      <ColorPicker value={color} onChange={setColor} />
    );
  }`}
/>