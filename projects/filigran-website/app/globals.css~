@tailwind base;
@tailwind components;
@tailwind utilities;

@layer utilities {
    body::-webkit-scrollbar {
      width: 16px;
    }
    body::-webkit-scrollbar-track {
        background: hsl(240 11% 96%);

    }
    body::-webkit-scrollbar-thumb {
        border-radius: 100vh;
        border: 4px solid  hsl(240 11% 96%);
        background: #62636ab3;
    }
    .dark {
        body::-webkit-scrollbar-track {
        background: hsl(230 100% 2.35%);
            }
        body::-webkit-scrollbar-thumb {
            border-radius: 100vh;
            border: 4px solid hsl(230 100% 2.35%);
            background: #62636ab3;
        }
    }
    body::-webkit-scrollbar-thumb:hover {
      background:  #62636a;
    }
}

@layer base {
  * {
    @apply border-border;
    font-family: var(--font-ibm-plex-sans), system-ui, sans-serif;
  }
  h1,
  h2,
  h3,
  h4,
  h5,
  h6 {
    font-family: var(--font-geologica), system-ui, sans-serif;
  }
  body {
    @apply bg-background text-foreground;
  }
}
